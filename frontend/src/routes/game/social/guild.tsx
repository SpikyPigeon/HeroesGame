import {createElement, Fragment, FunctionComponent} from "react";
import {Typography} from "@material-ui/core";

const Guild: FunctionComponent = () => {
	return <Fragment>
		<Typography variant="h4">Guild</Typography>
		<Typography variant="h6">Not Implemented</Typography>
	</Fragment>;
};

export default Guild;
